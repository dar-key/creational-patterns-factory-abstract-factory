# Creational Patterns: Factory Method & Abstract Factory

## Selected Domain

Game World Kit

## Structure

- `factorymethod`: Implements single-product creation via `EnemySpawner` subclasses
- `abstractfactory`: Implements family-product creation (WorldEnemy + Terrain) via `WorldFactory` implementations
